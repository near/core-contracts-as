{
  "version": "0.0.0",
  "name": "core-contracts",
  "license": "MIT",
  "scripts": {
    "build": "asb",
    "build:test": "asb --target debug",
    "asp": "asp",
    "jest": "jest",
    "test": "yarn build:test && yarn asp && yarn jest"
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "@types/node": "^14.6.2",
    "jest": "^26.4.2",
    "near-sdk-as": "^1.2.0",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  }
}
